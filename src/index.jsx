import React from "react";
import ReactDOM from "react-dom";
import Background  from "./views/Header/Header"
import './assets/styles/Main.css'

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Background/>
  </React.StrictMode>
);